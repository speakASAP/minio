# API routes for MinIO.
# Portal must use root endpoint (https://minio.alfares.cz) so SigV4 path matches.
# Root / is required for path-style S3 (e.g. /speakasap-records/...); deploy.sh patches proxy_pass so URI is preserved.
/
/minio/
/records/
