# API routes for MinIO (S3 API and Console).
# Proxied to minio container; nginx-microservice generates location blocks from this.
# /minio/ for Console; /records/ for path-style S3 (PUT /records/key) so path is preserved
# (location / with proxy_pass .../ would replace URI with / and break S3).
/minio/
/records/
