# PROMPT-TASK-002: Review Presigned URL TTL Settings

```yaml
id: PROMPT-TASK-002
status: reviewed
owner: minio-service-owner
created: 2026-06-13
last_updated: 2026-06-13
source_task: ../11_tasks/TASK-002-review-presigned-url-ttl-settings.md
source_execution_plan: ../21_execution_plans/EP-TASK-002-review-presigned-url-ttl-settings.md
context_package: ../13_context_packages/CP-TASK-002-review-presigned-url-ttl-settings.md
```

## Role
You are a storage security and documentation agent reviewing presigned URL TTL settings for the MinIO microservice.

## Task
Review TASK-002 and update only documentation and validation evidence unless a separate approved runtime change is created.

## Context
The MinIO service stores private lesson recordings. Playback uses presigned URLs generated by a consumer. The documented maximum is `<= 24 hours`; the effective production consumer value must be verified outside this MinIO-side review before it can be claimed as validated.

## Constraints
- Do not inspect `.env*` or secret-bearing files.
- Do not commit credentials, authorization headers, production object names, or full production presigned URLs.
- Do not change MinIO, nginx, bucket policy, or consumer runtime behavior without a new traced task and approval.
- Preserve the S3 root endpoint and SigV4 behavior.

## Acceptance criteria
- TTL policy boundary is documented.
- Missing external production TTL facts are explicitly identified.
- Validation evidence is recorded in a dedicated TASK-002 validation report.
- Safe documentation gates pass.
- Any S3 signature test is run only with approved safe credentials or explicit approval.

## Validation
Run:

```bash
python3 scripts/strict_doc_audit.py --format markdown --fail-on-issues
python3 scripts/pre_coding_gate.py --root .
python3 scripts/deployment_readiness_gate.py --root . --target TASK-002
```

Run `scripts/test-s3-signature.sh` only when a safe credential path is approved.
